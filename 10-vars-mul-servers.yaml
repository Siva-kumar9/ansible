- name: Variables
  hosts:  web
  tasks:
  - name: Print Variables from inventory
    ansible.builtin.debug:
      msg: "These Course is {{COURSE}} , Duration is {{DURATION}} , Trainer is {{TRAINER}}"