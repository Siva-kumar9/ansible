- name: Variables
  hosts:  local
  connection: local
  vars:
    NUM: 10
  tasks:
  - name: Print No greater than
    ansible.builtin.debug:
      msg: "Given Number {{NO}} is Greater than 10"
    when: NUM >= 10
  - name: Print No less than 10
    ansible.builtin.debug:
      msg: "Given Number {{NO}} is less than 10"
    when: NUM <= 10