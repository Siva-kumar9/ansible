- name: Install nginx
  hosts: all
  become: yes
  tasks:
  - name: Install
    ansible.builtin.package:
      name: nginx
      state: present
  - name: run nginx
    ansible.builtin.service:
      name: nginx
      state: started